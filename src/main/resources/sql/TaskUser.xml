<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="taskUserMapper">
    
    <select id="getTaskUserList" parameterType="int" resultType="taskDTO">
		select p.*,(select mem_name from member m where m.mem_no = p.mem_no) AS mem_name,(select task_no from task) AS task_no
		from participants p where p.cont_no = #{task_no}
    </select>

</mapper>
